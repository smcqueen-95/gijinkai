import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "placehold.co"], // Add all external image sources you use
  },
};

export default nextConfig;
